import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#101847',
      light: '#FFD8BE'
    },
    secondary: {
      main: '#FF5E5B',
    }
  },
});

export default theme;