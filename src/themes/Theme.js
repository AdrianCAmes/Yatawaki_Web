import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#101847',
      light: '#EFF2F9'
    },
    secondary: {
      main: '#8D298C',
    }
  },
});

export default theme;